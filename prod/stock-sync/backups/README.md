# SQLite Backups

The Dockerized worker writes consistent SQLite backups to this directory.
Generated backup and incident files are ignored by Git. Keep off-server copies
according to the production retention policy.
